apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: static-data2
  labels:
    app: static-data
spec:
  storageClassName: "default"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi

